# Frontend-only environment variables

# API base URL for the backend.
# Leave empty to use relative URLs (requires proxy or serving from same origin).
# Example: VITE_API_URL=http://localhost:7860
VITE_API_URL=https://dev-rexpro-omnichat-backend.hf.space
